# Loop to list comprehension examples

`ai_refusal.py`, `convertable.py` and `no_intention.py` are hand picked examples of what the intention should do.

`ai_examples` folder contains examples that ai generated.